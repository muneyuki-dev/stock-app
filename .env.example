# 固定ユーザーのIDまたはメールアドレス
AUTH_USER_ID=your-name@example.com

# `npm run auth:hash` の出力を設定する（平文パスワードは書かない）
AUTH_PASSWORD_HASH=scrypt$16384$8$1$replace_with_generated_hash

# 32文字以上のランダムな秘密文字列。例: `openssl rand -base64 32`
AUTH_SESSION_SECRET=replace_with_a_long_random_secret
